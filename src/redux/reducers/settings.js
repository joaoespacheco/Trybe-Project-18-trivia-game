const INITIAL_STATE = {
  questionNumbers: 5,
  selectedCategory: 'anyCategory',
  selectedDifficulty: '',
  selectedType: '',
};

const settings = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default settings;
