const INITIAL_STATE = {
  email: '',
  name: '',
  // token: '',
};

const session = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default session;
